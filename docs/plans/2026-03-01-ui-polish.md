# UI Polish & Visual Refinement Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Comprehensive visual polish — shared button components, pressed states, elevation, spacing normalization, card data styling, and loading/empty state improvements.

**Architecture:** Extract reusable `PrimaryButton`, `SecondaryButton`, `IconButton`, and `DeleteButton` into `src/components/Buttons.tsx`. Apply pressed-state feedback and elevation shadows across all screens. Normalize raw numeric spacing/typography values to theme tokens. Style card data rows and loading/empty states.

**Tech Stack:** React Native `Pressable`, `ActivityIndicator`, existing theme tokens from `src/theme.ts`.

---

### Task 1: Create Shared Button Components

**Files:**
- Create: `src/components/Buttons.tsx`

**Step 1: Create `src/components/Buttons.tsx` with all four button components**

```tsx
import { ActivityIndicator, Pressable, StyleSheet, Text } from 'react-native';

import { borderRadius, colors, elevation, spacing, typography } from '@/theme';

type PrimaryButtonProps = {
  label: string;
  onPress: () => void;
  disabled?: boolean;
};

export function PrimaryButton({ label, onPress, disabled }: PrimaryButtonProps): JSX.Element {
  return (
    <Pressable
      style={({ pressed }) => [
        styles.primaryButton,
        disabled && styles.primaryButtonDisabled,
        pressed && !disabled && styles.pressedOpacity,
      ]}
      onPress={onPress}
      disabled={disabled}
    >
      <Text style={styles.primaryButtonText}>{label}</Text>
    </Pressable>
  );
}

type SecondaryButtonProps = {
  label: string;
  onPress: () => void;
};

export function SecondaryButton({ label, onPress }: SecondaryButtonProps): JSX.Element {
  return (
    <Pressable
      style={({ pressed }) => [styles.secondaryButton, pressed && styles.pressedOpacityLight]}
      onPress={onPress}
    >
      <Text style={styles.secondaryButtonText}>{label}</Text>
    </Pressable>
  );
}

type IconButtonProps = {
  icon: string;
  onPress: () => void;
};

export function IconButton({ icon, onPress }: IconButtonProps): JSX.Element {
  return (
    <Pressable
      style={({ pressed }) => [styles.iconButton, pressed && styles.iconButtonPressed]}
      onPress={onPress}
    >
      <Text style={styles.iconText}>{icon}</Text>
    </Pressable>
  );
}

type DeleteButtonProps = {
  label: string;
  onPress: () => void;
};

export function DeleteButton({ label, onPress }: DeleteButtonProps): JSX.Element {
  return (
    <Pressable
      style={({ pressed }) => [styles.deleteButton, pressed && styles.pressedOpacity]}
      onPress={onPress}
    >
      <Text style={styles.deleteButtonText}>{label}</Text>
    </Pressable>
  );
}

const styles = StyleSheet.create({
  primaryButton: {
    alignItems: 'center',
    backgroundColor: colors.primary,
    borderRadius: borderRadius.md,
    paddingVertical: spacing.md,
    ...elevation.level1,
  },
  primaryButtonDisabled: {
    backgroundColor: colors.primaryContainer,
    ...elevation.level0,
  },
  primaryButtonText: {
    color: colors.onPrimary,
    ...typography.labelLarge,
  },
  secondaryButton: {
    alignItems: 'center',
    borderColor: colors.outline,
    borderRadius: borderRadius.md,
    borderWidth: 1,
    paddingVertical: spacing.md,
  },
  secondaryButtonText: {
    color: colors.onSurface,
    ...typography.labelMedium,
  },
  iconButton: {
    alignItems: 'center',
    backgroundColor: colors.surfaceVariant,
    borderRadius: borderRadius.xl,
    height: 28,
    justifyContent: 'center',
    width: 28,
  },
  iconButtonPressed: {
    backgroundColor: colors.outlineVariant,
  },
  iconText: {
    color: colors.onSurface,
    ...typography.labelLarge,
  },
  deleteButton: {
    alignItems: 'center',
    backgroundColor: colors.errorContainer,
    borderRadius: borderRadius.md,
    marginTop: spacing.sm,
    paddingVertical: spacing.md,
  },
  deleteButtonText: {
    color: colors.error,
    ...typography.labelLarge,
  },
  pressedOpacity: {
    opacity: 0.85,
  },
  pressedOpacityLight: {
    opacity: 0.7,
  },
});
```

**Step 2: Run typecheck**

Run: `npm run typecheck`
Expected: PASS

**Step 3: Commit**

```bash
git add src/components/Buttons.tsx
git commit -m "feat: add shared PrimaryButton, SecondaryButton, IconButton, DeleteButton components"
```

---

### Task 2: Update FormControls to Use Shared Buttons

**Files:**
- Modify: `src/components/FormControls.tsx`

The `formStyles` object currently exports `saveButton`, `saveButtonDisabled`, and `saveButtonText`. These will remain for now (removing them is done in Task 3-8 when each screen switches to `PrimaryButton`). In this task we normalize the remaining raw spacing values.

**Step 1: Normalize raw spacing in FormControls**

In `src/components/FormControls.tsx`, replace:
- `gap: 6` (line 154, `fieldGroup`) → `gap: spacing.sm`

That's the only raw numeric value. Everything else already uses theme tokens.

**Step 2: Run typecheck and tests**

Run: `npm run typecheck && npm test`
Expected: PASS

**Step 3: Commit**

```bash
git add src/components/FormControls.tsx
git commit -m "fix: normalize raw spacing values in FormControls"
```

---

### Task 3: Polish HomeScreen

**Files:**
- Modify: `src/screens/HomeScreen.tsx`

**Step 1: Add imports and replace buttons**

Add imports at top:
```tsx
import { ActivityIndicator } from 'react-native';
import { PrimaryButton, SecondaryButton } from '@/components/Buttons';
```

Replace the header buttons JSX (lines 43-48):
```tsx
<View style={styles.headerActions}>
  <PrimaryButton label="Add Mare" onPress={() => navigation.navigate('EditMare')} />
  <SecondaryButton label="Stallions" onPress={() => navigation.navigate('Stallions')} />
</View>
```

Replace loading text (line 51):
```tsx
{isLoading ? <ActivityIndicator color={colors.primary} size="large" /> : null}
```

Replace empty state text (line 54):
```tsx
{!isLoading && mares.length === 0 ? (
  <Text style={styles.emptyText}>No mares yet. Add your first mare.</Text>
) : null}
```

Add pressed state + elevation to mare rows (lines 63-78). Change `Pressable` style to use callback:
```tsx
<Pressable
  style={({ pressed }) => [styles.row, pressed && styles.rowPressed]}
  onPress={() => navigation.navigate('MareDetail', { mareId: item.id })}
>
```

Replace inline Edit button with:
```tsx
<Pressable
  style={({ pressed }) => [styles.inlineEditButton, pressed && styles.inlineEditPressed]}
  onPress={() => navigation.navigate('EditMare', { mareId: item.id })}
>
  <Text style={styles.inlineEditButtonText}>Edit</Text>
</Pressable>
```

**Step 2: Update StyleSheet**

Normalize raw values and add new styles:
- `headerActions.gap: 10` → `gap: spacing.md`
- `listContent.gap: 10` → `gap: spacing.md`
- `rowMain.gap: 2` → `gap: spacing.xs`
- `rowMain.marginRight: 10` → `marginRight: spacing.md`
- Add to `row`: `...elevation.level1`
- Remove `primaryButton`, `primaryButtonText`, `secondaryButton`, `secondaryButtonText` styles (now using shared components)
- `headerActions`: add `flex: 0` so buttons sit side by side; give each button wrapper a flex

Actually, the shared buttons need to work in a row. Since `PrimaryButton` has `alignItems: 'center'` but no fixed width, in a `flexDirection: 'row'` container each button needs `flex: 1`. We'll wrap them or adjust headerActions. Simplest: make `headerActions` flex row with `flex` children. But our shared buttons don't accept `style` prop. Let's keep the header buttons as inline `Pressable` with pressed states for now, since they need specific row layout. Only use shared `PrimaryButton` for full-width buttons.

**Revised approach for header buttons:** Keep inline but add pressed states and normalize spacing.

```tsx
<View style={styles.headerActions}>
  <Pressable
    style={({ pressed }) => [styles.primaryButton, pressed && styles.pressedOpacity]}
    onPress={() => navigation.navigate('EditMare')}
  >
    <Text style={styles.primaryButtonText}>Add Mare</Text>
  </Pressable>
  <Pressable
    style={({ pressed }) => [styles.secondaryButton, pressed && styles.pressedOpacityLight]}
    onPress={() => navigation.navigate('Stallions')}
  >
    <Text style={styles.secondaryButtonText}>Stallions</Text>
  </Pressable>
</View>
```

Add to StyleSheet:
```tsx
row: {
  alignItems: 'center',
  backgroundColor: colors.surfaceVariant,
  borderColor: colors.outline,
  borderRadius: borderRadius.lg,
  borderWidth: 1,
  flexDirection: 'row',
  justifyContent: 'space-between',
  padding: spacing.md,
  ...elevation.level1,
},
rowPressed: {
  opacity: 0.92,
},
primaryButton: {
  backgroundColor: colors.primary,
  borderRadius: borderRadius.md,
  paddingHorizontal: spacing.lg,
  paddingVertical: spacing.md,
},
primaryButtonText: {
  color: colors.onPrimary,
  ...typography.labelLarge,
},
secondaryButton: {
  backgroundColor: colors.secondaryContainer,
  borderRadius: borderRadius.md,
  paddingHorizontal: spacing.lg,
  paddingVertical: spacing.md,
},
secondaryButtonText: {
  color: colors.onSurface,
  ...typography.labelMedium,
},
pressedOpacity: {
  opacity: 0.85,
},
pressedOpacityLight: {
  opacity: 0.7,
},
inlineEditPressed: {
  opacity: 0.7,
},
emptyText: {
  color: colors.onSurfaceVariant,
  textAlign: 'center',
  paddingVertical: spacing.xl,
  ...typography.bodyMedium,
},
```

Add `elevation` import from `@/theme`.

**Step 3: Run typecheck and tests**

Run: `npm run typecheck && npm test`
Expected: PASS

**Step 4: Commit**

```bash
git add src/screens/HomeScreen.tsx
git commit -m "feat: polish HomeScreen with pressed states, elevation, and spacing normalization"
```

---

### Task 4: Polish MareDetailScreen

**Files:**
- Modify: `src/screens/MareDetailScreen.tsx`

This is the most complex screen. Changes:

**Step 1: Add imports**

```tsx
import { ActivityIndicator } from 'react-native';
import { IconButton, PrimaryButton } from '@/components/Buttons';
import { elevation } from '@/theme';
```

**Step 2: Replace icon button helper**

Replace `renderEditIconButton` (lines 94-98):
```tsx
const renderEditIconButton = (onPress: () => void): JSX.Element => (
  <IconButton icon={'\u270E'} onPress={onPress} />
);
```

Remove `iconButton` and `iconText` from StyleSheet.

**Step 3: Replace "Add" buttons with PrimaryButton**

In each tab section, replace:
```tsx
<Pressable style={styles.primaryButton} onPress={...}>
  <Text style={styles.primaryButtonText}>Add Daily Log</Text>
</Pressable>
```
With:
```tsx
<PrimaryButton label="Add Daily Log" onPress={() => navigation.navigate('DailyLogForm', { mareId })} />
```

Same for "Add Breeding Record", "Add Pregnancy Check", "Add Foaling Record".

Remove `primaryButton` and `primaryButtonText` from StyleSheet.

**Step 4: Add card data row styling**

Add a local `CardRow` helper at the top of the component (inside the function, before the return):
```tsx
const renderCardRow = (label: string, value: string | number | null | undefined): JSX.Element => (
  <View style={styles.cardRow}>
    <Text style={styles.cardLabel}>{label}</Text>
    <Text style={styles.cardValue}>{value ?? '-'}</Text>
  </View>
);
```

Replace unstyled `<Text>` lines in each card. For example in daily logs:
```tsx
{renderCardRow('Teasing', log.teasingScore ?? '-')}
{renderCardRow('Edema', log.edema ?? '-')}
{renderCardRow('Right ovary', log.rightOvary || '-')}
{renderCardRow('Left ovary', log.leftOvary || '-')}
```

Same pattern for breeding records:
```tsx
{renderCardRow('Method', record.method)}
{renderCardRow('Stallion', stallionNameById[record.stallionId] ?? 'Unknown')}
{record.collectionDate ? renderCardRow('Collection', record.collectionDate) : null}
```

Pregnancy checks:
```tsx
{renderCardRow('Result', check.result)}
{renderCardRow('Heartbeat', check.heartbeatDetected ? 'Yes' : 'No')}
{renderCardRow('Days post-breeding', daysPost ?? '-')}
```

Foaling records:
```tsx
{renderCardRow('Outcome', record.outcome)}
{renderCardRow('Foal sex', record.foalSex ?? '-')}
{record.complications ? renderCardRow('Complications', record.complications) : null}
```

**Step 5: Style empty states and loading**

Replace loading text:
```tsx
{isLoading ? <ActivityIndicator color={colors.primary} size="large" /> : null}
```

Replace empty state texts in each tab:
```tsx
{dailyLogs.length === 0 ? <Text style={styles.emptyText}>No daily logs yet.</Text> : null}
```

Same for all four tabs.

**Step 6: Add pressed state to tab buttons**

```tsx
<Pressable
  key={tab.value}
  style={({ pressed }) => [styles.tabButton, active ? styles.tabButtonActive : null, pressed && !active && styles.tabPressed]}
  onPress={() => setActiveTab(tab.value)}
>
```

**Step 7: Normalize StyleSheet and add new styles**

```tsx
headerCard: {
  backgroundColor: colors.surfaceVariant,
  borderColor: colors.outline,
  borderRadius: borderRadius.lg,
  borderWidth: 1,
  gap: spacing.xs,
  marginBottom: spacing.md,
  padding: spacing.md,
  ...elevation.level2,
},
headerName: {
  ...typography.titleMedium,
  fontWeight: '700',
},
headerLine: {
  color: colors.onSurfaceVariant,
  ...typography.bodySmall,
},
listWrap: {
  gap: spacing.md,
},
card: {
  backgroundColor: colors.surface,
  borderColor: colors.outline,
  borderRadius: borderRadius.lg,
  borderWidth: 1,
  gap: spacing.xs,
  padding: spacing.md,
  ...elevation.level1,
},
cardHeader: {
  alignItems: 'center',
  flexDirection: 'row',
  justifyContent: 'space-between',
  marginBottom: spacing.xs,
},
cardRow: {
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
},
cardLabel: {
  color: colors.onSurfaceVariant,
  ...typography.bodySmall,
},
cardValue: {
  color: colors.onSurface,
  ...typography.bodyMedium,
},
emptyText: {
  color: colors.onSurfaceVariant,
  textAlign: 'center',
  paddingVertical: spacing.xl,
  ...typography.bodyMedium,
},
tabPressed: {
  opacity: 0.7,
},
scrollContent: {
  paddingBottom: spacing.xxxl,
},
```

Replace the `secondaryButton` marginTop `spacing.sm` — keep as is, it's fine.

**Step 8: Run typecheck and tests**

Run: `npm run typecheck && npm test`
Expected: PASS

**Step 9: Commit**

```bash
git add src/screens/MareDetailScreen.tsx
git commit -m "feat: polish MareDetailScreen with elevation, card styling, pressed states, and ActivityIndicator"
```

---

### Task 5: Polish StallionManagementScreen

**Files:**
- Modify: `src/screens/StallionManagementScreen.tsx`

**Step 1: Add imports and replace buttons**

Add:
```tsx
import { ActivityIndicator } from 'react-native';
import { DeleteButton, PrimaryButton, SecondaryButton } from '@/components/Buttons';
import { elevation } from '@/theme';
```

Replace save button with `PrimaryButton`, cancel with `SecondaryButton`, delete with `DeleteButton`.

Replace loading text with `ActivityIndicator`.

Replace empty state with styled text.

**Step 2: Add pressed states to stallion cards**

The stallion card itself isn't tappable (edit/delete are separate buttons), so just add elevation:

```tsx
card: {
  ...existing styles...,
  padding: spacing.md,
  ...elevation.level1,
},
```

**Step 3: Normalize all raw values**

- `listWrap.gap: 10` → `spacing.md`
- `listWrap.paddingBottom: 10` → `spacing.md`
- `card.padding: 10` → `spacing.md`
- `cardMain.gap: 2` → `spacing.xs`
- `cardMain.marginRight: 10` → `spacing.md`
- `inlineButton.paddingHorizontal: 10` → `spacing.md`
- `inlineButton.paddingVertical: 7` → `spacing.sm`
- `deleteButton.paddingHorizontal: 10` → `spacing.md`
- `deleteButton.paddingVertical: 7` → `spacing.sm`
- `actionRow.gap: 10` → `spacing.md`

Add pressed states to inline Edit/Delete buttons.

Add `emptyText` style same as HomeScreen.

**Step 4: Run typecheck and tests**

Run: `npm run typecheck && npm test`
Expected: PASS

**Step 5: Commit**

```bash
git add src/screens/StallionManagementScreen.tsx
git commit -m "feat: polish StallionManagementScreen with shared buttons, elevation, and spacing normalization"
```

---

### Task 6: Polish Form Screens (EditMare, DailyLog, Breeding, Pregnancy, Foaling)

**Files:**
- Modify: `src/screens/EditMareScreen.tsx`
- Modify: `src/screens/DailyLogFormScreen.tsx`
- Modify: `src/screens/BreedingRecordFormScreen.tsx`
- Modify: `src/screens/PregnancyCheckFormScreen.tsx`
- Modify: `src/screens/FoalingRecordFormScreen.tsx`

All five form screens follow the same pattern. For each:

**Step 1: Replace save button with PrimaryButton**

Import `PrimaryButton` and `DeleteButton` from `@/components/Buttons`.

Replace:
```tsx
<Pressable
  style={[formStyles.saveButton, isSaving && formStyles.saveButtonDisabled]}
  onPress={handleSave}
  disabled={isSaving}
>
  <Text style={formStyles.saveButtonText}>{isSaving ? 'Saving...' : 'Save'}</Text>
</Pressable>
```
With:
```tsx
<PrimaryButton
  label={isSaving ? 'Saving...' : 'Save'}
  onPress={handleSave}
  disabled={isSaving}
/>
```

**Step 2: Replace delete button with DeleteButton (DailyLog, Breeding, Pregnancy, Foaling only)**

Replace:
```tsx
<Pressable style={styles.deleteButton} onPress={handleDelete}>
  <Text style={styles.deleteButtonText}>Delete</Text>
</Pressable>
```
With:
```tsx
<DeleteButton label="Delete" onPress={handleDelete} />
```

Remove local `deleteButton`/`deleteButtonText` styles from each screen.

**Step 3: Replace loading text with ActivityIndicator**

Import `ActivityIndicator` from `react-native`.

Replace `<Text>Loading daily log...</Text>` with:
```tsx
<ActivityIndicator color={colors.primary} size="large" />
```

Same for all screens' loading states.

**Step 4: Style the unstyled info text in PregnancyCheckFormScreen**

The "Days post-breeding" display (line ~239) and "Heartbeat is forced to No" message need styling:
```tsx
<View style={localStyles.infoRow}>
  <Text style={localStyles.infoLabel}>Days post-breeding: {daysPostBreeding === null ? '-' : `${daysPostBreeding}`}</Text>
</View>
```
With styles:
```tsx
infoRow: {
  backgroundColor: colors.surfaceVariant,
  borderRadius: borderRadius.md,
  padding: spacing.md,
},
infoLabel: {
  color: colors.onSurfaceVariant,
  ...typography.bodyMedium,
},
```

**Step 5: Run typecheck and tests**

Run: `npm run typecheck && npm test`
Expected: PASS

**Step 6: Commit**

```bash
git add src/screens/EditMareScreen.tsx src/screens/DailyLogFormScreen.tsx src/screens/BreedingRecordFormScreen.tsx src/screens/PregnancyCheckFormScreen.tsx src/screens/FoalingRecordFormScreen.tsx
git commit -m "feat: polish form screens with shared buttons, ActivityIndicator, and styled info text"
```

---

### Task 7: Clean Up Unused formStyles

**Files:**
- Modify: `src/components/FormControls.tsx`

After all screens use shared button components, the `formStyles.saveButton`, `saveButtonDisabled`, and `saveButtonText` exports are no longer needed.

**Step 1: Check that no file imports these styles**

Search for `formStyles.saveButton` across all files. If no references remain, remove those three entries from `formStyles` in `FormControls.tsx`. Keep `formStyles.form` as it's still used by all form screens for the `ScrollView` `contentContainerStyle`.

If references remain, leave them and skip this task.

**Step 2: Run typecheck and tests**

Run: `npm run typecheck && npm test`
Expected: PASS

**Step 3: Commit**

```bash
git add src/components/FormControls.tsx
git commit -m "chore: remove unused saveButton formStyles after shared button migration"
```

---

### Task 8: Final Verification

**Step 1: Run full quality checks**

Run: `npm run typecheck && npm test`
Expected: All pass.

**Step 2: Visual smoke test**

Run: `npm start` or `npm run android`
Check each screen:
- HomeScreen: mare rows have shadows, buttons respond to press
- MareDetailScreen: header card has shadow, record cards have shadow, data rows are styled
- StallionManagementScreen: cards have shadows, spacing is consistent
- All form screens: save button has shadow, delete button styled, loading shows spinner
